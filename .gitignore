# ── Dataset outputs (generated data files, not the folder structure) ───────
dataset/**/*.png
dataset/**/*.exr
dataset/**/*.hdr
dataset/metadata.jsonl

# ── 3D Meshes (large binary files, distribute separately) ─────────────────
cloth_meshes/*.obj
cloth_meshes/*.mtl

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# ── macOS ─────────────────────────────────────────────────────────────────
.DS_Store
